# Nạp khi chạy `npm run dev:https` (vite --mode https).
# Bật same-origin: mọi API đi qua vite proxy cùng origin (xem vite.config.js) →
# test luồng Đăng nhập Facebook HTTPS ngay trên máy, khỏi deploy production.
VITE_SAME_ORIGIN=1
